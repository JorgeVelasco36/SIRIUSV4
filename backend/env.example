# Database Configuration
# SQLite (no requiere instalación) - Ya configurado por defecto
DATABASE_URL=sqlite:///./sirius_v4.db

# Si prefieres usar PostgreSQL, descomenta y configura:
# DATABASE_URL=postgresql://usuario:password@localhost:5432/sirius_v4

# Supabase (para leer archivos de valoración)
# Obtén tu API Key en el panel de Supabase: Settings > API > anon key
# Obtén tu URL del proyecto en: Settings > API > Project URL
SUPABASE_URL=https://mwyltxcgjxsrdmgsuysv.supabase.co
SUPABASE_API_KEY=tu_api_key_aqui
SUPABASE_TABLE_PIP=BD_PIP
SUPABASE_TABLE_PRECIA=BD_Precia

# MongoDB Atlas (deprecated - ya no se usa)
# MONGODB_URI=
# MONGODB_DATABASE=sirius_v4
# MONGODB_COLLECTION=valuation_files

# Microsoft Graph API (SharePoint) - OPCIONAL - Solo si aún necesitas SharePoint
# Obtén estas credenciales en: https://portal.azure.com
# Si no usas SharePoint, puedes dejar estos valores vacíos
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
AZURE_TENANT_ID=
SHAREPOINT_SITE_ID=
SHAREPOINT_DRIVE_ID=

# LLM Configuration (OpenAI)
# Obtén tu clave en: https://platform.openai.com/api-keys
# IMPORTANTE: Esta clave es obligatoria para que el asistente funcione
OPENAI_API_KEY=tu_openai_api_key
LLM_MODEL=gpt-4
LLM_TEMPERATURE=0.3

# Application
# Genera cualquier texto aleatorio para SECRET_KEY (puede ser cualquier texto largo)
SECRET_KEY=mi-clave-secreta-12345-abcde
ENVIRONMENT=development
LOG_LEVEL=INFO

# API Configuration
API_V1_PREFIX=/api/v1
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

